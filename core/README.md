# Core Engines

The system is centered around five engines:

- `agent-engine`
- `skill-engine`
- `workflow-engine`
- `task-engine`
- `memory-engine`

The current TypeScript reference implementation lives under [src/core](D:\Google_download\HardieSkill\ai-startup-os\src\core), while this `core/` directory defines the product-facing architecture and long-term repository shape.
